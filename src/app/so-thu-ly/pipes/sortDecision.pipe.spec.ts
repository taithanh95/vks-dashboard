import { SortDecisionPipe } from './sortDecision.pipe';

describe('SortPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDecisionPipe();
    expect(pipe).toBeTruthy();
  });
});
